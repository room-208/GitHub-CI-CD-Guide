name: Debug Log

on: push

jobs:
    log:
        runs-on: ubuntu-latest
        steps:
            - run: echo "::debug::This is a debug log"
    
    log-trace:
        runs-on: ubuntu-latest
        steps:
            - run: |
                set -x
                date
                hostname